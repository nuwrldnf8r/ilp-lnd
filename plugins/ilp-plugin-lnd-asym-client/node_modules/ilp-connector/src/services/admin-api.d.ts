import reduct = require('reduct');
export default class AdminApi {
    private accounts;
    private config;
    private middlewareManager;
    private routingTable;
    private routeBroadcaster;
    private rateBackend;
    private stats;
    private server?;
    private routes;
    constructor(deps: reduct.Injector);
    listen(): void;
    private handleRequest(req, res);
    private getStatus();
    private getRoutingStatus();
    private getAccountStatus();
    private getBalanceStatus();
    private postBalance(url, _data);
    private getBackendStatus();
    private getStats();
    private getAlerts();
    private deleteAlert(url, _);
}
