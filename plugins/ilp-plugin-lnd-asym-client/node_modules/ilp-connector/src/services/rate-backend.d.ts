import reduct = require('reduct');
import { BackendInstance, SubmitPaymentParams } from '../types/backend';
export default class RateBackend implements BackendInstance {
    protected backend: BackendInstance;
    private accounts;
    constructor(deps: reduct.Injector);
    connect(): Promise<void>;
    getRate(sourceAccount: string, destinationAccount: string): Promise<number>;
    submitPayment(params: SubmitPaymentParams): Promise<void>;
    getStatus(): Promise<{}>;
}
