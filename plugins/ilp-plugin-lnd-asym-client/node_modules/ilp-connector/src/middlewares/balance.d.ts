import { Middleware, MiddlewareServices, Pipelines } from '../types/middleware';
import BigNumber from 'bignumber.js';
export default class BalanceMiddleware implements Middleware {
    private getInfo;
    private sendMoney;
    private balances;
    constructor(opts: {}, {getInfo, sendMoney}: MiddlewareServices);
    applyToPipelines(pipelines: Pipelines, accountId: string): Promise<void>;
    getStatus(): {
        accounts: {};
    };
    modifyBalance(accountId: string, _amountDiff: BigNumber.Value): BigNumber;
    private getBalance(accountId);
    private maybeSettle(accountId);
}
