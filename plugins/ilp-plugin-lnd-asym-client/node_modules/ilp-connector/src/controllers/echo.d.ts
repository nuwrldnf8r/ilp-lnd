/// <reference types="node" />
import reduct = require('reduct');
import { IlpPrepare } from 'ilp-packet';
export default class EchoController {
    private config;
    private routeBuilder;
    constructor(deps: reduct.Injector);
    handle(data: Buffer, sourceAccount: string, {parsedPacket, outbound}: {
        parsedPacket: IlpPrepare;
        outbound: (data: Buffer, accountId: string) => Promise<Buffer>;
    }): Promise<Buffer>;
}
