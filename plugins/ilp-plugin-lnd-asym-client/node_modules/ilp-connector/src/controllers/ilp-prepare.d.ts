/// <reference types="node" />
import reduct = require('reduct');
export default class IlpPrepareController {
    private accounts;
    private routeBuilder;
    private backend;
    private peerProtocolController;
    private echoController;
    constructor(deps: reduct.Injector);
    sendData(packet: Buffer, sourceAccount: string, outbound: (data: Buffer, accountId: string) => Promise<Buffer>): Promise<Buffer>;
}
