/// <reference types="node" />
import RouteBroadcaster from '../services/route-broadcaster';
import reduct = require('reduct');
import { IlpPrepare } from 'ilp-packet';
export default class CcpController {
    protected routeBroadcaster: RouteBroadcaster;
    constructor(deps: reduct.Injector);
    handle(data: Buffer, sourceAccount: string, {parsedPacket}: {
        parsedPacket: IlpPrepare;
    }): Promise<Buffer>;
    handleRouteControl(data: Buffer, sourceAccount: string): Promise<Buffer>;
    handleRouteUpdate(data: Buffer, sourceAccount: string): Promise<Buffer>;
}
