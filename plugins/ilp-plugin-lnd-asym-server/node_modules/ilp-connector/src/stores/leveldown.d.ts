import { LevelUp } from 'levelup';
import { StoreInstance } from '../types/store';
declare class LeveldownStore implements StoreInstance {
    db: LevelUp;
    constructor({db, path}: {
        db?: LevelUp;
        path?: string;
    });
    get(key: string): Promise<any>;
    put(key: string, value: string): Promise<any>;
    del(key: string): Promise<any>;
}
export = LeveldownStore;
