export default class Stats {
    private counters;
    private meters;
    meter(key: string): void;
    counter(key: string, value: number): void;
    getStatus(): {
        counters: {
            [k: string]: number;
        };
        meters: {
            [k: string]: number;
        };
    };
}
