import { Middleware, Pipelines } from '../types/middleware';
export default class DeduplicateMiddleware implements Middleware {
    private packetCache;
    applyToPipelines(pipelines: Pipelines, accountId: string): Promise<void>;
    private cleanupCache();
}
