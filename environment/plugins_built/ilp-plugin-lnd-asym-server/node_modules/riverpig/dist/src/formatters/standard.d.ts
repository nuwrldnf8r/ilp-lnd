import { LogEntry } from '../interfaces/log-entry';
import { CompleteLoggerConfig } from '../interfaces/logger-config';
declare const _default: (config: CompleteLoggerConfig) => (entry: LogEntry) => string;
export default _default;
