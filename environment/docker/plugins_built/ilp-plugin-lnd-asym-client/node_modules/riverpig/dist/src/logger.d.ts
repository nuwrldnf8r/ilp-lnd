/// <reference types="node" />
import { LoggerConfig } from './interfaces/logger-config';
import { LogMethod } from './interfaces/log-method';
export declare class Logger {
    debug: LogMethod;
    info: LogMethod;
    warn: LogMethod;
    error: LogMethod;
    private stream;
    private format;
    constructor(namespace: string, config0?: LoggerConfig);
    setOutputStream(stream: NodeJS.WritableStream): void;
    setStringFormatter(format: (message: string, ...elements: any[]) => string): void;
    private write;
}
